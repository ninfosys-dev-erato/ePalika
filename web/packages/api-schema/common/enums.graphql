enum Scope { MUNICIPALITY WARD }
enum Priority { LOW MEDIUM HIGH URGENT }
enum IntakeChannel { COUNTER POSTAL EMAIL EDARTA_PORTAL COURIER }
enum DispatchChannel { POSTAL COURIER EMAIL HAND_DELIVERY EDARTA_PORTAL }
enum AddressStatus {
  CAPTURED
  NORMALIZED
  GEO_VALIDATED
  VERIFIED
  REJECTED
  HISTORICAL
  ANONYMIZED
}
