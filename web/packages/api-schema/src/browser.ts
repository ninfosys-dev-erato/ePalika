/**
 * ---------------------------------------------------------------------------
 * 🧬  ePalika API Schema — Browser Entry Point
 * ---------------------------------------------------------------------------
 * Browser-compatible export that imports the pre-built schema string
 * ---------------------------------------------------------------------------
 */

export { typeDefs } from "./schema-string";
