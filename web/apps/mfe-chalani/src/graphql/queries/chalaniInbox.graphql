query MyChalaniInbox($pagination: PaginationInput) {
  myChalaniInbox(pagination: $pagination) {
    edges {
      node {
        id
        subject
        status
        recipient {
          name
        }
        createdAt
      }
    }
  }
}
